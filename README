This is a document measurement thingy.

HOWTO:

# pull in the submodule of mochiweb, only needed once
git submodule init
git submodule update
cd deps/mochiweb-src; make; cd -

# this will be your normal compile/run/test loop
make
./start-dev.sh
. resty
resty resty http://127.0.0.1:8000/

GET

POST /foo data
